= timecolumn

Adds two columns to issue queries, one that contains the time spent on this
issue and one that contains the remaining time (estimated-spent).

== Installation

cd $REDMINE_HOME/vendor/plugins
hg clone https://bitbucket.org/gocept/redmine_timecolumn
